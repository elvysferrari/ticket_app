import 'package:flutter/cupertino.dart';

class MenuItemModel{
  IconData icon;
  String iconText;
  Color iconColor;
  Color textColor;

  MenuItemModel({required this.icon, required this.iconText, required this.iconColor, required this.textColor});
}